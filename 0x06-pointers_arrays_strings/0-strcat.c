#include "main.h"
/**
**_strcat - concatenates two strings
*@dest: pointer destination
*@src: pointer source
*Return: void
*/
char *_strcat(char *dest, char *src)
{
int n = -1, j;
for (j = 0; dest[i] != '\0'; j++)
;
do {
	n++;
	dest[i] = src[n];
	j++;
} while (src[n] != '\0');

return (dest);
}
